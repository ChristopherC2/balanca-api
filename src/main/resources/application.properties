spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# ---------------------------------------------------------------------------
# Algoritmo de estabilizacao de peso (hardware ESP32)
# ---------------------------------------------------------------------------
balanca.pesagem.min-leituras=5
balanca.pesagem.tolerancia-kg=0.5
# 5 minutos em ms
balanca.pesagem.max-idade-leitura-ms=300000

# ---------------------------------------------------------------------------
# Controle de transacoes e financeiro
# ---------------------------------------------------------------------------
# 1 hora em ms
balanca.transacao.expiracao-chave-ms=3600000
balanca.transacao.margem-lucro=0.15
balanca.transacao.simulacao-minutos-inicio=2

# ---------------------------------------------------------------------------
# Autenticacao
# ---------------------------------------------------------------------------
# 10 minutos em ms
balanca.auth.token-expiracao-ms=600000